# Copy this to .env and adjust paths for your system

# External data path for large datasets/models not in git
# This gets mounted to /ros_ws/external_data inside container
ROS_DEMO_DATA=/mnt/wdblack_sn850x_2tb/projects/ros-perception-demo-data

# Display for GUI (Gazebo, RViz)
# Run `echo $DISPLAY` on your Linux PC (logged in locally, not SSH) to find this
# Usually :0 for primary display, :1 for secondary/VNC
DISPLAY=:0
